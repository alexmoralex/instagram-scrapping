export const BASE_URL = 'https://instasupersave.com/'
export const BASE_PROFILE_URL = `${BASE_URL}api/ig/profile/` // GET - username in path
export const BASE_MEDIA_URL = `${BASE_URL}api/convert` // POST - url in body (profile url or post url)
export const BASE_STORIES_URL = `${BASE_URL}api/ig/stories/` // GET - user id in path or story url in query param
export const BASE_HIGHLIGHTS_URL = `${BASE_URL}api/ig/highlights/` // GET - user id in path
export const BASE_USER_URL = `${BASE_URL}api/ig/userInfoByUsername/` // GET - username in path
